# Порядок заголовков на страницах контактов

Да, исправимо — и почти всё уже сделано. Я посмотрел живую страницу `/uk/kontakty`: там всё ещё старая опубликованная версия, где сразу после главного заголовка идёт заголовок формы третьего уровня, а второго уровня нет вообще. Отсюда и замечание о «перескоке» уровней.

В текущей (ещё не опубликованной) версии страницы контактов уже есть подзаголовки второго уровня: способы связи, что будет после заявки, что написать в заявке, частые вопросы, ссылки. Так что после публикации порядок становится правильным.

## Что сделаю дополнительно

1. Заголовок формы («Замовити консультацію» / «Book a call» / «Umów rozmowę») переведу со третьего уровня на второй — он равнозначен соседним разделам страницы, а не вложен в них. Внешне размер и стиль остаются прежними.
2. Проверю сборку и порядок заголовков на всех трёх языках: главный заголовок → разделы второго уровня → вопросы FAQ третьего уровня.
3. Затем нужна публикация, чтобы живой сайт получил новую структуру.

## Технические детали

- Правка в `src/components/contact/ContactForm.tsx`: `<h3>` → `<h2>` с сохранением классов.
- Проверка через сборку и разбор итогового HTML для `/uk/kontakty`, `/en/contact`, `/pl/kontakt`.
