# 📊 Справочник событий dataLayer для Leonforge

Быстрая справка по всем событиям, которые отправляются из кода в `window.dataLayer`.

---

## 📋 Таблица всех событий

| № | Событие | Категория | Когда происходит | Файл | Функция |
|---|---------|-----------|------------------|------|---------|
| 1 | `form_submit` | Lead Generation | Отправка формы обратной связи | `ContactForm.tsx` | `handleSubmit` |
| 2 | `portfolio_view` | Portfolio Engagement | Клик на проект в портфолио | `PortfolioSection.tsx` | `openProjectPopup` |
| 3 | `cta_click` | CTA | Клик на CTA кнопку | `CTAButton.tsx` | `scrollToContact` |
| 4 | `scroll` | Engagement | Прокрутка страницы на 25/50/75/100% | `Analytics.tsx` | `trackScroll` |
| 5 | `phone_click` | Contact | Клик на номер телефона | `ContactInfoCard.tsx` | `handleContactClick` |
| 6 | `email_click` | Contact | Клик на email | `ContactInfoCard.tsx` | `handleContactClick` |
| 7 | `telegram_click` | Contact | Клик на Telegram | `ContactInfoCard.tsx` | `handleContactClick` |
| 8 | `conversion` | Conversion | Важное действие (конверсия) | `Analytics.tsx` | `trackConversion` |

---

## 1. form_submit

### Описание
Срабатывает при успешной отправке формы обратной связи.

### Параметры
```javascript
{
  event: 'form_submit',
  event_category: 'Lead Generation',
  event_label: 'Contact Form' // тип формы
}
```

### Где происходит
- **Файл**: `src/components/contact/ContactForm.tsx`
- **Функция**: `handleSubmit`
- **Строка**: ~95

### Код
```typescript
const { trackFormSubmission } = useAnalytics();

const handleSubmit = async (e: React.FormEvent) => {
  // ... отправка формы
  trackFormSubmission('Contact Form');
};
```

### Использование в маркетинге
- **Главная конверсия** для Google Ads
- Отслеживание заявок
- Расчет Cost Per Lead (CPL)
- Создание аудиторий для ремаркетинга

---

## 2. portfolio_view

### Описание
Срабатывает при клике на проект в разделе портфолио.

### Параметры
```javascript
{
  event: 'portfolio_view',
  event_category: 'Portfolio Engagement',
  event_label: 'CheatAIC' // название проекта
}
```

### Где происходит
- **Файл**: `src/components/PortfolioSection.tsx`
- **Функция**: `openProjectPopup`
- **Строка**: ~80

### Код
```typescript
const { trackPortfolioView } = useAnalytics();

const openProjectPopup = (project: any, type: 'desktop' | 'mobile') => {
  trackPortfolioView(project.title);
  // ... открытие попапа
};
```

### Использование в маркетинге
- Вовлеченность пользователей
- Какие проекты интересны клиентам
- Создание аудитории "Интересовались портфолио"
- Оптимизация показа проектов

---

## 3. cta_click

### Описание
Срабатывает при клике на любую CTA кнопку (Call-to-Action).

### Параметры
```javascript
{
  event: 'cta_click',
  event_category: 'CTA',
  event_label: 'Безкоштовна консультація', // текст кнопки
  section: 'cta_button' // секция на странице
}
```

### Где происходит
- **Файл**: `src/components/CTAButton.tsx`
- **Функция**: `scrollToContact`
- **Строка**: ~17

### Код
```typescript
const { trackCTAClick } = useAnalytics();

const scrollToContact = () => {
  trackCTAClick(t('hero.consultation'), 'cta_button');
  document.getElementById('contact')?.scrollIntoView({ behavior: 'smooth' });
};
```

### Использование в маркетинге
- Намерение к конверсии
- A/B тестирование CTA кнопок
- Воронка конверсии (CTA → Form)
- Оптимизация текста кнопок

---

## 4. scroll

### Описание
Срабатывает при прокрутке страницы на определенный процент (25%, 50%, 75%, 100%).

### Параметры
```javascript
{
  event: 'scroll',
  event_category: 'Engagement',
  event_label: '50%', // процент прокрутки
  value: 50 // числовое значение
}
```

### Где происходит
- **Файл**: `src/components/SEO/Analytics.tsx`
- **Функция**: `trackScroll`
- **Строка**: ~123

### Код
```typescript
const { trackScroll } = useAnalytics();

// Вызывается автоматически при прокрутке
trackScroll(50); // 50% страницы прокручено
```

### Использование в маркетинге
- Вовлеченность контента
- Какие секции читают пользователи
- Сегментация по глубине взаимодействия
- Оптимизация длины страницы

---

## 5. phone_click

### Описание
Срабатывает при клике на номер телефона в контактной информации.

### Параметры
```javascript
{
  event: 'phone_click',
  event_category: 'Contact',
  event_label: 'Phone Number'
}
```

### Где происходит
- **Файл**: `src/components/contact/ContactInfoCard.tsx`
- **Функция**: `handleContactClick`
- **Строка**: ~46

### Код
```typescript
const { trackPhoneClick } = useAnalytics();

const handleContactClick = (type?: string) => {
  if (type === 'phone') {
    trackPhoneClick();
  }
};
```

### Использование в маркетинге
- Конверсия "Клик на контакт"
- Популярность канала связи
- Создание аудитории "Заинтересованные в звонке"
- Оптимизация мобильной версии

---

## 6. email_click

### Описание
Срабатывает при клике на email адрес в контактной информации.

### Параметры
```javascript
{
  event: 'email_click',
  event_category: 'Contact',
  event_label: 'Email'
}
```

### Где происходит
- **Файл**: `src/components/contact/ContactInfoCard.tsx`
- **Функция**: `handleContactClick`
- **Строка**: ~44

### Код
```typescript
const { trackEmailClick } = useAnalytics();

const handleContactClick = (type?: string) => {
  if (type === 'email') {
    trackEmailClick();
  }
};
```

### Использование в маркетинге
- Конверсия "Клик на контакт"
- Популярность email как канала связи
- Создание аудитории для email-рассылок
- Сравнение эффективности каналов

---

## 7. telegram_click

### Описание
Срабатывает при клике на ссылку Telegram в контактной информации.

### Параметры
```javascript
{
  event: 'telegram_click',
  event_category: 'Contact',
  event_label: 'Telegram'
}
```

### Где происходит
- **Файл**: `src/components/contact/ContactInfoCard.tsx`
- **Функция**: `handleContactClick`
- **Строка**: ~48

### Код
```typescript
const { trackTelegramClick } = useAnalytics();

const handleContactClick = (type?: string) => {
  if (type === 'telegram') {
    trackTelegramClick();
  }
};
```

### Использование в маркетинге
- Конверсия "Клик на контакт"
- Популярность Telegram как канала связи
- Сегментация аудитории по предпочтениям
- Оптимизация каналов коммуникации

---

## 8. conversion

### Описание
Универсальное событие для отслеживания важных конверсий через Google Ads.

### Параметры
```javascript
{
  event: 'conversion',
  send_to: 'AW-XXXXXXXXXX/conversion_label',
  value: 500,
  currency: 'UAH'
}
```

### Где происходит
- **Файл**: `src/components/SEO/Analytics.tsx`
- **Функция**: `trackConversion`
- **Строка**: ~160

### Код
```typescript
const { trackConversion } = useAnalytics();

// Пример использования
trackConversion('form_submit_label', 500);
```

### Использование в маркетинге
- Прямое отслеживание конверсий в Google Ads
- Привязка ценности к действию
- Smart Bidding стратегии
- ROAS оптимизация

---

## 🔧 Как использовать это в GTM

### Создание триггера для любого события

1. GTM → Triggers → New
2. Trigger Type: **Custom Event**
3. Event name: **название_события** (например, `form_submit`)
4. This trigger fires on: **All Custom Events**

### Создание переменной для параметра

1. GTM → Variables → New
2. Variable Type: **Data Layer Variable**
3. Data Layer Variable Name: **название_параметра** (например, `event_label`)
4. Data Layer Version: **Version 2**

---

## 📈 Примеры использования для аудиторий

### Аудитория 1: "Горячие лиды"
**Условия:**
- Событие `portfolio_view` (просмотрели проекты)
- И событие `cta_click` (кликнули на CTA)
- Но НЕ событие `form_submit` (не отправили форму)

**Использование:** Ремаркетинг с предложением скидки

---

### Аудитория 2: "Интересующиеся контактами"
**Условия:**
- Событие `email_click` ИЛИ `telegram_click` ИЛИ `phone_click`
- Но НЕ событие `form_submit`

**Использование:** Упрощенная форма с минимальными полями

---

### Аудитория 3: "Вовлеченные пользователи"
**Условия:**
- Событие `scroll` с `event_label` >= 75%
- И время на сайте > 2 минуты

**Использование:** Lookalike аудитории для таргетинга

---

## 🎯 Воронка конверсии

```
1. page_view (просмотр страницы)
   ↓
2. scroll 50% (прочитали половину)
   ↓
3. portfolio_view (просмотрели проекты)
   ↓
4. cta_click (кликнули на CTA)
   ↓
5. email_click / telegram_click (попытка связаться)
   ↓
6. form_submit (отправили заявку) ✅ КОНВЕРСИЯ
```

**Анализируйте, где теряются пользователи, и оптимизируйте эти шаги!**

---

## 📝 Шпаргалка для разработчиков

### Как добавить новое событие

```typescript
// 1. В файле Analytics.tsx добавьте функцию
const trackNewEvent = (customParam: string) => {
  window.dataLayer?.push({
    event: 'new_event_name',
    event_category: 'Custom Category',
    event_label: customParam,
  });
  logger.info(`New event tracked: ${customParam}`);
};

// 2. Добавьте в return useAnalytics
return {
  // ... существующие
  trackNewEvent,
};

// 3. Используйте в компоненте
const { trackNewEvent } = useAnalytics();
trackNewEvent('some value');
```

### Как проверить событие в консоли браузера

```javascript
// Откройте DevTools → Console
// Введите:
dataLayer

// Вы увидите массив всех событий
// Последнее событие:
dataLayer[dataLayer.length - 1]
```

---

## 🆘 Troubleshooting

### Событие не отправляется

1. **Проверьте консоль**: `console.log(window.dataLayer)` - должен быть массив
2. **Проверьте GTM Preview**: откройте GTM Preview и посмотрите, срабатывает ли событие
3. **Проверьте условия**: убедитесь, что функция вызывается (добавьте `console.log` перед `dataLayer.push`)

### Событие отправляется дважды

1. **Проверьте триггеры GTM**: возможно, есть дублирующиеся триггеры
2. **Проверьте код**: убедитесь, что `trackEvent` вызывается только один раз
3. **Проверьте старые теги**: удалите или отключите старые теги в GTM

---

## 📚 Полезные ссылки

- [GTM Setup Guide](./GTM_SETUP_GUIDE.md) - Полная инструкция по настройке
- [Migration Checklist](./GTM_MIGRATION_CHECKLIST.md) - Чек-лист миграции
- [Google Tag Manager Help](https://support.google.com/tagmanager)
- [GA4 Events Documentation](https://support.google.com/analytics/answer/9322688)
- [Google Ads Conversion Tracking](https://support.google.com/google-ads/answer/1722022)

---

**✅ Используйте этот справочник для быстрого поиска информации о событиях!**
